import React from 'react';
import { Route, Switch } from 'react-router-dom';

const CssRoutes = () => {
    return (
        <Switch>
            <Route />
        </Switch>
    );
};

export default CssRoutes;
